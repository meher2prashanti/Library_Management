<h1><%= @subject.name -%></h1>

<ul>
  <% @subject.book.each do |c| %>
      <li><%= link_to c.title, :action => "show", :id => c.id -%></li>
  <% end %>
</ul>

<%= link_to 'Back', {:action => 'list'},:class => 'btn btn-primary btn-sm' %>